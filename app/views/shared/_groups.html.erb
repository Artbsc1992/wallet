<section>
  <div class="flex justify-center p-5">
    <img src="<%= group.icon %>" alt="group icon" class="w-24 h-20" />
    <div>
      <h2 class="text-2xl font-bold text-gray-700 self-center"><%= group.name %></h2>
      <h2 class="text-2xl font-bold text-gray-700">Total: $<%=group.total_amount%></h2>
    </div>
  </div>
</section>